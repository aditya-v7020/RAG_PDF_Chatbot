import { BrainCircuit } from "lucide-react";
import { useApp } from "../context/AppContext.jsx";
export default function ModelInfo() { const { setup } = useApp(); return <section className="mt-6 border-t border-slate-100 pt-5"><div className="flex items-center gap-2 text-sm font-bold text-slate-700"><BrainCircuit size={15} className="text-brand-600" /> AI configuration</div><div className="mt-3 space-y-2 text-xs text-slate-500"><p className="flex items-center justify-between gap-3"><span>Chat model</span><code className="max-w-[150px] truncate rounded bg-slate-100 px-1.5 py-1 text-[10px] text-slate-600">{setup.llm_model || "—"}</code></p><p className="flex items-center justify-between gap-3"><span>Embeddings</span><code className="max-w-[150px] truncate rounded bg-slate-100 px-1.5 py-1 text-[10px] text-slate-600">{setup.embedding_model || "—"}</code></p></div></section>; }
