import { BookOpen, FileImage, FileText, Layers3, MessageSquareText, Sparkles } from "lucide-react";
import { useEffect } from "react";
import MetricCard from "../components/MetricCard.jsx";
import { useApp } from "../context/AppContext.jsx";
export default function DashboardPage() { const { dashboard, refreshDashboard, messages } = useApp(); useEffect(() => { refreshDashboard(); }, [refreshDashboard]); const metrics = [{ value: dashboard.document_count, label: "Documents indexed", icon: BookOpen, tone: "brand" }, { value: dashboard.total_chunks, label: "Searchable chunks", icon: Layers3, tone: "violet" }, { value: dashboard.total_images, label: "Visuals extracted", icon: FileImage, tone: "cyan" }, { value: messages.filter((message) => message.role === "user").length, label: "Questions asked", icon: MessageSquareText, tone: "amber" }]; return <div className="mx-auto max-w-6xl px-4 py-6 sm:px-6 lg:px-8"><div className="rounded-3xl bg-gradient-to-br from-slate-950 via-slate-900 to-brand-900 px-6 py-7 text-white shadow-xl shadow-slate-200 sm:px-8"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="eyebrow text-cyan-200">Knowledge library</p><h2 className="mt-2 text-2xl font-bold tracking-tight sm:text-3xl">Your document intelligence at a glance</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">Track what is available to the assistant and how much material is ready for semantic search.</p></div><div className="flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-2 text-xs font-medium text-cyan-100"><Sparkles size={14} /> Search-ready workspace</div></div></div><div className="grid grid-cols-2 gap-3 pt-5 lg:grid-cols-4">{metrics.map((metric) => <MetricCard key={metric.label} {...metric} />)}</div><section className="mt-8"><div className="mb-4 flex items-end justify-between gap-4"><div><p className="eyebrow">Indexed collection</p><h3 className="mt-1 text-lg font-bold text-slate-900">Documents in your library</h3></div><span className="text-xs text-slate-400">Source files used for answers</span></div>{dashboard.documents.length === 0 ? <div className="rounded-2xl border border-dashed border-slate-300 bg-white px-5 py-10 text-center shadow-sm"><FileText size={26} className="mx-auto text-slate-300" /><p className="mt-3 text-sm font-semibold text-slate-700">Your library is empty</p><p className="mt-1 text-xs text-slate-400">Upload PDFs from the document panel to begin building it.</p></div> : <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{dashboard.documents.map((document, index) => <div key={document.name} className={`flex items-center gap-3 px-4 py-4 sm:px-5 ${index !== dashboard.documents.length - 1 ? "border-b border-slate-100" : ""}`}><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-red-50 text-red-500"><FileText size={18} /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-800">{document.name}</p><p className="mt-0.5 text-xs text-slate-400">Indexed {document.added_at || "recently"}</p></div><div className="text-right"><p className="text-sm font-bold text-slate-700">{document.chunks}</p><p className="text-[11px] text-slate-400">chunks</p></div></div>)}</div>}</section></div>; }
